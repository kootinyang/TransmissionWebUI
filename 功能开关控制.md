# Transmission Web Interface 功能开关控制指南

## 快速切换

在浏览器控制台（F12 → Console）中执行以下命令：

### 1. 种子列表假数据开关

```javascript
// 启用假数据
localStorage.setItem('mock_torrents_enabled', 'true');

// 禁用假数据
localStorage.setItem('mock_torrents_enabled', 'false');

// 查看当前状态
localStorage.getItem('mock_torrents_enabled');

// 清除设置（恢复代码默认）
localStorage.removeItem('mock_torrents_enabled');
```

### 2. 连接失败弹窗开关

```javascript
// 显示弹窗（恢复原始行为）
localStorage.setItem('show_connection_alert', 'true');

// 屏蔽弹窗
localStorage.setItem('show_connection_alert', 'false');

// 查看当前状态
localStorage.getItem('show_connection_alert');

// 清除设置（恢复代码默认）
localStorage.removeItem('show_connection_alert');
```

---

## URL 参数方式

在地址栏添加参数（适合临时切换，不会保存）：

```
// 启用假数据
http://localhost:9091/transmission/web/?mock_torrents=true

// 禁用假数据
http://localhost:9091/transmission/web/?mock_torrents=false
```

---

## 代码配置方式

直接修改代码文件（永久生效）：

### 修改假数据开关

编辑 `mock-torrents.js`：

```javascript
// ==================== 配置开关 ====================
const CONFIG = {
    // 主开关：设置为 true 启用假数据，false 禁用
    enable: true,  // ← 修改这里
    
    // 是否启用实时更新
    enableRealtimeUpdate: true,
    
    // 更新间隔（毫秒）
    updateInterval: 1000
};
// =================================================
```

### 修改弹窗开关

编辑 `transmission-app.js`，找到以下代码：

```javascript
// 查找这一行：
if(localStorage.getItem("show_connection_alert")!=="true")

// 修改为：
if(localStorage.getItem("show_connection_alert")!=="true") // 改为 !=="false" 默认屏蔽
```

---

## 开关优先级

1. **URL 参数**（最高优先级，临时生效）
2. **localStorage**（中等优先级，持久保存）
3. **代码配置**（最低优先级，默认行为）

---

## 一键切换脚本

将以下代码保存为书签，点击即可切换：

### 切换假数据
```javascript
javascript:(function(){var curr=localStorage.getItem('mock_torrents_enabled');var next=curr==='true'?'false':'true';localStorage.setItem('mock_torrents_enabled',next);alert('Mock data: '+(next==='true'?'ON':'OFF')+'\nRefresh page to apply');})();
```

### 切换连接弹窗
```javascript
javascript:(function(){var curr=localStorage.getItem('show_connection_alert');var next=curr==='true'?'false':'true';localStorage.setItem('show_connection_alert',next);alert('Connection alert: '+(next==='true'?'ON':'OFF')+'\nRefresh page to apply');})();
```

---

## 状态查看

在浏览器控制台执行以下代码查看当前状态：

```javascript
console.log('=== 功能开关状态 ===');
console.log('假数据:', localStorage.getItem('mock_torrents_enabled') || '使用代码默认');
console.log('连接弹窗:', localStorage.getItem('show_connection_alert') || '使用代码默认');
console.log('===================');
```

---

## 恢复默认

清除所有自定义设置：

```javascript
localStorage.removeItem('mock_torrents_enabled');
localStorage.removeItem('show_connection_alert');
alert('所有设置已恢复默认，请刷新页面');
```

---

## 常见问题

**Q: 修改后为什么不生效？**
A: 需要刷新页面才能生效。

**Q: 如何知道当前是哪个开关在起作用？**
A: 打开浏览器控制台，查看 console 输出的日志信息。

**Q: 多人使用时如何统一配置？**
A: 建议直接修改代码中的默认配置值，这样所有人使用相同的设置。

**Q: 可以同时在多个设备上同步设置吗？**
A: localStorage 不会同步，需要在每台设备上单独设置，或者使用 URL 参数。
