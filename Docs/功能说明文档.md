# Transmission Web Interface 功能说明文档

## 一、项目概述

这是一个 **Transmission BT下载客户端的Web管理界面**，允许用户通过浏览器远程管理Transmission下载任务。

### 技术栈
- **前端框架**: 原生 JavaScript (无框架依赖)
- **构建工具**: 已打包为单文件
- **样式**: CSS3
- **通信**: RPC API 与 Transmission 守护进程通信

---

## 二、界面布局

### 2.1 整体结构

```
┌─────────────────────────────────────────────────────────────┐
│  [Open] [Delete] | [Start] [Stop] | [Inspector]      [...] │  ← 工具栏
├─────────────────────────────────────────────────────────────┤
│  Show [All▼] [Tracker▼] [🔍 Filter] [×]     ↓↓0 B/s  ↑↑0 B/s 🐢│  ← 状态栏
├─────────────────────────────────────────────────────────────┤
│                                                             │
│                    种子列表区域                              │  ← 主工作区
│                                                             │
├─────────────────────────────────────────────────────────────┤
│        Drag your torrent files here to add them...          │  ← 拖放区域
└─────────────────────────────────────────────────────────────┘
```

---

## 三、功能模块详解

### 3.1 工具栏 (Toolbar)

| 按钮 | 功能 | 快捷键 | 状态 |
|------|------|--------|------|
| **Open** | 打开种子文件/添加下载任务 | Alt+O | 始终可用 |
| **Delete** | 删除选中的种子 | Alt+Delete | 需选中种子 |
| **Start** | 开始/恢复选中的种子 | Alt+R | 需选中种子 |
| **Stop** | 暂停选中的种子 | Alt+P | 需选中种子 |
| **Inspector** | 打开详情检查器 | Alt+I | 始终可用 |
| **... (More)** | 显示更多选项菜单 | Alt+M | 始终可用 |

---

### 3.2 状态栏 (Status Bar)

| 元素 | 功能说明 |
|------|----------|
| **Show** | 显示筛选标签 |
| **状态筛选下拉框** | 按状态过滤种子 (全部/下载中/做种中/暂停等) |
| **Tracker筛选下拉框** | 按Tracker来源过滤 |
| **搜索框** | 按名称过滤种子 |
| **清除按钮 (×)** | 清空搜索关键词 |
| **筛选计数** | 显示当前筛选结果数量 |
| **下载速度显示** | 实时显示总下载速度 |
| **上传速度显示** | 实时显示总上传速度 |
| **乌龟按钮 (🐢)** | 切换限速模式 ( alternate speed limits ) |

---

### 3.3 种子列表 (Torrent List)

#### 3.3.1 显示模式
- **标准模式**: 显示完整的种子信息
- **紧凑模式** (Compact rows): 精简行高显示更多内容

#### 3.3.2 种子信息展示
每个种子条目显示：
- 下载进度条
- 种子名称
- 大小信息
- 下载/上传速度
- 状态图标 (下载中/做种中/暂停/校验中等)
- 剩余时间估算
- 做种数/连接数

#### 3.3.3 选择操作
- **单击**: 选择单个种子
- **Ctrl/Cmd + 单击**: 多选/取消选择
- **Shift + 单击**: 范围选择

---

### 3.4 右键菜单 / 更多选项菜单

| 功能 | 说明 | 快捷键 |
|------|------|--------|
| **Resume** | 恢复下载 | 无 |
| **Resume Now** | 立即恢复 (忽略队列限制) | 无 |
| **Pause** | 暂停下载 | 无 |
| **Remove from list** | 从列表中移除 | Delete |
| **Trash data and remove** | 删除数据并移除 | Shift+Delete |
| **Move to Top** | 移动到队列顶部 | 无 |
| **Move Up** | 在队列中上移 | 无 |
| **Move Down** | 在队列中下移 | 无 |
| **Move to Bottom** | 移动到队列底部 | 无 |
| **Ask tracker for more peers** | 重新向Tracker请求 peers | 无 |
| **Verify Local Data** | 校验本地数据 | 无 |
| **Set Location** | 更改保存位置 | 无 |
| **Rename** | 重命名种子 | 无 |
| **Labels** | 设置标签 | 无 |
| **Select All** | 全选 | Ctrl+A |
| **Deselect All** | 取消全选 | D |
| **Copy name** | 复制种子名称 | 无 |

---

### 3.5 检查器 (Inspector)

点击 **Inspector** 按钮或双击种子打开详情面板，包含以下标签页：

#### 3.5.1 General (常规信息)
- 种子名称
- 哈希值
- 保存路径
- 总大小 / 已完成大小
- 创建时间
- 最后活动时间
- 下载/上传总量
- 做种时间

#### 3.5.2 Peers (连接信息)
- 已连接 peers 列表
- 每个 peer 的地址、客户端、传输速度
- 连接加密状态
- 上传/下载比例

#### 3.5.3 Trackers
- Tracker URL
- 最后请求时间
- 下次请求时间
- 响应状态
- Peers 数量

#### 3.5.4 Files (文件列表)
- 种子内所有文件
- 每个文件的下载进度
- 文件大小
- 优先级设置
- 下载/跳过选项

---

### 3.6 添加种子对话框 (Add Torrent)

通过 **Open** 按钮或拖放文件触发：

| 功能项 | 说明 |
|--------|------|
| **选择文件** | 选择 `.torrent` 文件 |
| **URL输入** | 输入磁力链接或种子URL |
| **保存路径** | 选择下载保存位置 |
| **开始选项** | 是否立即开始下载 |
| **文件选择** | 选择性下载特定文件 |

---

### 3.7 设置对话框 (Preferences)

包含以下设置类别：

#### 3.7.1 下载设置 (Downloading)
- 默认保存路径
- 未完成文件添加 `.part` 后缀
- 下载完成后自动开始做种

#### 3.7.2 速度限制 (Speed)
- 全局下载速度限制
- 全局上传速度限制
- 限速时间段设置

#### 3.7.3 做种设置 (Seeding)
- 做种比率限制
- 做种时间限制
- 达到限制后暂停

#### 3.7.4 网络设置 (Network)
- 监听端口
- 端口转发 (UPnP/NAT-PMP)
- 加密模式

#### 3.7.5 队列设置 (Queue)
- 同时下载任务数
- 同时做种任务数

---

### 3.8 统计信息 (Statistics)

显示Transmission运行统计：
- 当前活动种子数
- 暂停种子数
- 总下载/上传量 (本次会话/历史总计)
- 运行时间

---

### 3.9 连接状态提示 (Connection Status)

当与Transmission后端服务断开连接时，会显示连接失败提示：

| 提示类型 | 说明 |
|----------|------|
| **连接失败弹窗** | 当RPC请求失败时显示，提示"Could not connect to the server. You may need to reload the page to reconnect." |
| **自动关闭** | 连接恢复后自动关闭弹窗 |

> ⚠️ **注意**: 此功能当前已屏蔽，如需恢复请在浏览器控制台执行：
> ```javascript
> localStorage.setItem('show_connection_alert', 'true');
> ```
> 或在 URL 中添加 `?connection_alert=true`

---

### 3.10 拖放功能 (Drag & Drop)

支持直接从文件管理器拖放：
- `.torrent` 文件
- 磁力链接文本
- 多个文件同时拖放

拖放区域显示提示文字：
> "Drag your torrent files here to add them to Transmission."

---

## 四、快捷键汇总

| 快捷键 | 功能 |
|--------|------|
| **Alt+O** | 打开/添加种子 |
| **Alt+Delete** | 删除选中种子 |
| **Alt+R** | 开始/恢复选中种子 |
| **Alt+P** | 暂停选中种子 |
| **Alt+I** | 切换检查器 |
| **Alt+M** | 显示更多选项菜单 |
| **Ctrl+A** | 全选 |
| **D** | 取消全选 |
| **Delete** | 从列表移除 |
| **Shift+Delete** | 删除数据并移除 |

---

## 五、视觉与辅助功能

### 5.1 显示选项
- **紧凑行模式**: 减小行高，显示更多内容
- **高对比度UI**: 提高可访问性

### 5.2 图标资源
- `favicon.ico` / `favicon.png` - 网站图标
- `webclip-icon.png` - iOS主屏幕图标

---

## 六、文件结构

```
TransmissionWebUI/
├── index.html              # 主页面
├── transmission-app.css    # 样式文件
├── transmission-app.js     # 主逻辑文件 (已打包)
├── transmission-app.css.map    # CSS Source Map
├── transmission-app.js.map     # JS Source Map
├── images/                 # 图标资源
│   ├── favicon.ico
│   ├── favicon.png
│   └── webclip-icon.png
└── default.pen             # 设计文件
```

---

## 七、调试功能开关

### 7.1 种子列表假数据

用于前端样式调试的假数据生成器，可在无后端服务时显示测试数据。

**开关方式：**
1. **浏览器控制台**: `localStorage.setItem('mock_torrents_enabled', 'true')`
2. **URL 参数**: `?mock_torrents=true`
3. **代码修改**: 编辑 `mock-torrents.js` 中的 `CONFIG.enable`

**配置项：**
- `enable`: 主开关
- `enableRealtimeUpdate`: 是否启用速度和进度的实时波动
- `updateInterval`: 更新间隔（毫秒）

### 7.2 连接失败弹窗

控制当后端服务不可用时是否显示连接失败提示。

**开关方式：**
1. **浏览器控制台**: `localStorage.setItem('show_connection_alert', 'true')`
2. **代码修改**: 编辑 `transmission-app.js`

---

## 八、后端通信

前端通过 **Transmission RPC API** 与后端守护进程通信：
- 获取种子列表
- 控制种子状态 (开始/暂停/删除)
- 获取/设置配置参数
- 实时更新种子状态

---

*文档生成日期: 2026-04-03*
